const FEEDBACK_PREFIX = 'feedbacks';

export enum FeedbackThunkPrefix {
    FETCH_CREATE_FEEDBACK = `${FEEDBACK_PREFIX}/fetchCreateFeedback`,
    FETCH_FEEDBACKS = `${FEEDBACK_PREFIX}/fetchFeedbacks`,
}
